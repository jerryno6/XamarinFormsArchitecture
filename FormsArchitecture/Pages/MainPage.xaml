<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:FormsArchitecture.ViewModels;assembly=FormsArchitecture"
             x:DataType="viewModels:MainViewModel"
             x:Class="FormsArchitecture.Pages.MainPage">

    <StackLayout>
        <Label Text="Enter BaseUrl for gRPC" 
               HorizontalTextAlignment="Center" 
               TextColor="Black" FontSize="36"/>
        
        <Entry Text="{Binding BaseUrl}"
               Placeholder="https://localhost:5000"
               FontSize="Title" 
               HorizontalOptions="Fill"/>
        
        <Button Text="Greet" 
                Padding="20,20,20,20"
                Command="{Binding LoadCommand}"/>
        
        <Label FontSize="16" Padding="30,24,30,0" Text="{Binding GreetMessage}" />
    </StackLayout>

</ContentPage>
