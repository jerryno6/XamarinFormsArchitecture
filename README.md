# A Playground for Xamarin.Forms

### Platforms:
- iOS
- Android
- UWP
- macOS
- .net core 3.1 for backend

# Xamarin Forms with gRPC:
- Branch `gRPC-Xamarin`

### Limitation:
- MAC: does not work, because the server cannot run on MAC, MAC does not support TSL. We will run it on kubernetes
- UWP : works
